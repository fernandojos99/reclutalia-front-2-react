# Backend contra el que pega el front.
# Sin esta variable se usa el de producción (Vercel), que es lo que queremos en el despliegue.
# Para desarrollo full-local, copia este archivo a `.env` y levanta el Express en :4000.
VITE_API_URL=http://localhost:4000/api
